# 数组越界

## 问题描述

数组访问超出声明的范围，导致 RE 或未定义行为。

## 典型错误代码

```cpp
int a[100];
// 访问 a[100] 或 a[-1]
```

## 正确写法

```cpp
int a[105]; // 适当开大数组
// 注意循环边界
```

## 常见场景

- 循环上界写错（`<=` 写成 `<` 或反过来）
- 多组数据时数组大小不够
- 图论中边数组开小（应开 2 倍）

## 出现频率

- 整体：高
- 涉及学生：（填写）