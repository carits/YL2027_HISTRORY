#include <bits/stdc++.h>
using namespace std;
int n, m, k;
int main(){
    freopen("plan.in", "r", stdin);
    freopen("plan.out", "w", stdout);
    cin >> n >> m >> k;
    for(int i = 1; i <= n; i++){
        int c;
        cin >> c;
        for(int j = 1; j <= c; j++){
            int d;
            cin >> d;
        }
    }
    cout << k;
    return 0;
}
