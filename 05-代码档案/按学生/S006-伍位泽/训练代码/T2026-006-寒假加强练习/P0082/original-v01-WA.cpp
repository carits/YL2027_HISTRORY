#include <bits/stdc++.h>
using namespace std;
int a[200005] , b[200005];
int main ( )
{
    int t , n;
    cin >> t;
    while (t--)
    {
        cin >> n;
        for (int i=1;i<=n;i++)
        {
            cin >> a[i];
        }
        for (int i=1;i<=n;i++)
        {
            cin >> b[i];
        }
        
    }
    return 0;
}