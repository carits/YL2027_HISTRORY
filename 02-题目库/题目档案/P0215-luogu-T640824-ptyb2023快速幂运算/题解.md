# P0215 【ptyb2023】快速幂运算 题解

## 一、题意简化

本题可以归入 **数论**。核心任务是：根据输入限制，选择合适的数据结构或算法，避免直接写出超出复杂度的暴力做法。

## 二、思路分析

底数和模数先从 $k$ 进制转成十进制。指数很长，不能整体转出；从左到右读指数数字，若当前指数为 $E$，读入数字 $d$ 后新指数为 $Ek+d$，于是结果更新为 $res^k\cdot a^dmod m$。最后把结果转回 $k$ 进制。

## 三、算法步骤

1. 读入题目数据。
2. 根据题意维护必要的状态或预处理信息。
3. 按照上述思路完成计算。
4. 输出题目要求的答案，注意空格、换行和特殊输出格式。

## 四、正确性说明

算法直接围绕题目中的不变量或等价转化进行：每一步维护的状态都与原问题中的真实状态一一对应；当状态更新结束时，得到的就是题目要求的最终结果。对于排序、枚举、模拟、动态规划或数学转化类题目，代码中的比较规则、转移规则或公式均来自题意本身，因此不会改变答案含义。

## 五、复杂度分析

$O(|n|\log k)$。

## 六、参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll parse_base(const string &s, int k) {
    ll x = 0;
    for (char c : s) x = x * k + (c - '0');
    return x;
}

ll mod_pow(ll a, ll b, ll mod) {
    if (mod == 1) return 0;
    ll r = 1 % mod;
    a %= mod;
    while (b) {
        if (b & 1) r = (__int128)r * a % mod;
        a = (__int128)a * a % mod;
        b >>= 1;
    }
    return r;
}

string to_base(ll x, int k) {
    if (x == 0) return "0";
    string s;
    while (x) {
        s.push_back(char('0' + x % k));
        x /= k;
    }
    reverse(s.begin(), s.end());
    return s;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int k;
    string A, M, N;
    cin >> k >> A >> M >> N;
    ll a = parse_base(A, k);
    ll mod = parse_base(M, k);
    ll res = 1 % mod;
    ll base = a % mod;
    for (char c : N) {
        int d = c - '0';
        res = mod_pow(res, k, mod) * mod_pow(base, d, mod) % mod;
    }
    cout << to_base(res, k) << '\n';
    return 0;
}
```
