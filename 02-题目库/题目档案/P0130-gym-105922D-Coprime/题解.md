# P0130 Coprime 题解

## 一、题意简化

本题可以归入 **数论**。核心任务是：根据输入限制，选择合适的数据结构或算法，避免直接写出超出复杂度的暴力做法。

## 二、思路分析

需要找 $x<z<y$ 且 $z$ 与 $x,y$ 都互质。先分解 $x,y$ 的所有不同质因子，问题变为找区间内不被这些质数整除的数。用容斥计算前缀中合法数个数，再二分第一个合法位置。

## 三、算法步骤

1. 读入题目数据。
2. 根据题意维护必要的状态或预处理信息。
3. 按照上述思路完成计算。
4. 输出题目要求的答案，注意空格、换行和特殊输出格式。

## 四、正确性说明

算法直接围绕题目中的不变量或等价转化进行：每一步维护的状态都与原问题中的真实状态一一对应；当状态更新结束时，得到的就是题目要求的最终结果。对于排序、枚举、模拟、动态规划或数学转化类题目，代码中的比较规则、转移规则或公式均来自题意本身，因此不会改变答案含义。

## 五、复杂度分析

分解约 $O(\sqrt{x}+\sqrt{y})$，容斥只枚举乘积不超过上界的质因子子集。

## 六、参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;
using int64 = long long;

vector<int64> factor(int64 x) {
    vector<int64> p;
    for (int64 d = 2; d * d <= x; d += (d == 2 ? 1 : 2)) {
        if (x % d == 0) {
            p.push_back(d);
            while (x % d == 0) x /= d;
        }
    }
    if (x > 1) p.push_back(x);
    return p;
}

long long count_coprime(long long n, const vector<long long> &p) {
    if (n <= 0) return 0;
    long long ans = n;
    function<void(int, __int128, int)> dfs = [&](int pos, __int128 prod, int parity) {
        for (int i = pos; i < (int)p.size(); i++) {
            __int128 np = prod * p[i];
            if (np > n) continue;
            long long term = n / (long long)np;
            if (parity == 0) ans -= term;
            else ans += term;
            dfs(i + 1, np, parity ^ 1);
        }
    };
    dfs(0, 1, 0);
    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--) {
        long long x, y;
        cin >> x >> y;
        vector<long long> p = factor(x), q = factor(y);
        p.insert(p.end(), q.begin(), q.end());
        sort(p.begin(), p.end());
        p.erase(unique(p.begin(), p.end()), p.end());
        long long l = x + 1, r = y - 1;
        if (count_coprime(r, p) - count_coprime(l - 1, p) == 0) {
            cout << -1 << '\n';
            continue;
        }
        long long lo = l, hi = r;
        while (lo < hi) {
            long long mid = (lo + hi) >> 1;
            if (count_coprime(mid, p) - count_coprime(l - 1, p) > 0) hi = mid;
            else lo = mid + 1;
        }
        cout << lo << '\n';
    }
    return 0;
}
```
