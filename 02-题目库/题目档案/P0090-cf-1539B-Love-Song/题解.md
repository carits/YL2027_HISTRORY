# P0090 Love Song 题解

## 一、题意简化

本题的核心是：给定一个只包含小写字母的字符串。

## 二、算法设计

把每个字符转成权值，建立前缀和 pre。

区间 [l, r] 的答案就是 pre[r] - pre[l - 1]。

## 三、正确性说明

算法直接围绕题目要求维护必要状态。每次转移或统计都只使用已经确定的信息，并且覆盖所有可能答案；最终取到的结果与题目定义一致。

## 四、复杂度分析

预处理 O(n)，每次询问 O(1)。

## 五、参考代码

见 [AC代码.cpp](AC代码.cpp)。
