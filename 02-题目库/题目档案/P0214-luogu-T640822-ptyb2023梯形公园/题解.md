# P0214 【ptyb2023】梯形公园 题解

## 一、题意简化

本题可以归入 **数学**。核心任务是：根据输入限制，选择合适的数据结构或算法，避免直接写出超出复杂度的暴力做法。

## 二、思路分析

枚举哪两根作为上底和下底，剩下两根作为腰。设上下底差为 $d$，两腰为 $p,q$，它们与 $d$ 构成一个三角形；同时两腰与下底夹角不超过 $90^\circ$，等价于两个水平投影均非负。合法时用三角形高作为梯形高计算面积。

## 三、算法步骤

1. 读入题目数据。
2. 根据题意维护必要的状态或预处理信息。
3. 按照上述思路完成计算。
4. 输出题目要求的答案，注意空格、换行和特殊输出格式。

## 四、正确性说明

算法直接围绕题目中的不变量或等价转化进行：每一步维护的状态都与原问题中的真实状态一一对应；当状态更新结束时，得到的就是题目要求的最终结果。对于排序、枚举、模拟、动态规划或数学转化类题目，代码中的比较规则、转移规则或公式均来自题意本身，因此不会改变答案含义。

## 五、复杂度分析

$O(1)$。

## 六、参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<double> a(4);
    for (double &x : a) cin >> x;
    double best = -1;
    for (int u = 0; u < 4; u++) {
        for (int l = 0; l < 4; l++) {
            if (u == l || a[u] >= a[l]) continue;
            vector<int> rem;
            for (int i = 0; i < 4; i++) if (i != u && i != l) rem.push_back(i);
            double top = a[u], bottom = a[l], p = a[rem[0]], q = a[rem[1]];
            double d = bottom - top;
            if (d <= 0) continue;
            if (p + q <= d || fabs(p - q) >= d) continue;
            double alpha = (p * p - q * q + d * d) / (2 * d);
            double beta = d - alpha;
            if (alpha < -1e-9 || beta < -1e-9) continue;
            double h2 = p * p - alpha * alpha;
            if (h2 <= 1e-9) continue;
            double h = sqrt(max(0.0, h2));
            best = max(best, (top + bottom) * h / 2);
        }
    }
    if (best < 0) cout << "Impossible!\n";
    else cout << fixed << setprecision(3) << best + 1e-9 << '\n';
    return 0;
}
```
