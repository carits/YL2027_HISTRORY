# P0158 【青苗杯2023小学组】跑步相遇 题解

## 一、题意简化

本题可以归入 **数学**。核心任务是：根据输入限制，选择合适的数据结构或算法，避免直接写出超出复杂度的暴力做法。

## 二、思路分析

把往返跑展开成长度为 $2l$ 的环。实际位置相等等价于展开坐标满足 $s_1\equiv s_2\pmod{2l}$ 或 $s_1\equiv -s_2\pmod{2l}$。分别求两类一次同余方程的最小非负时间。

## 三、算法步骤

1. 读入题目数据。
2. 根据题意维护必要的状态或预处理信息。
3. 按照上述思路完成计算。
4. 输出题目要求的答案，注意空格、换行和特殊输出格式。

## 四、正确性说明

算法直接围绕题目中的不变量或等价转化进行：每一步维护的状态都与原问题中的真实状态一一对应；当状态更新结束时，得到的就是题目要求的最终结果。对于排序、枚举、模拟、动态规划或数学转化类题目，代码中的比较规则、转移规则或公式均来自题意本身，因此不会改变答案含义。

## 五、复杂度分析

$O(1)$。

## 六、参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long double L;
    cin >> L;
    long double x1, v1, d1, x2, v2, d2;
    cin >> x1 >> v1 >> d1;
    cin >> x2 >> v2 >> d2;
    long double u1 = d1 > 0.5 ? v1 : -v1;
    long double u2 = d2 > 0.5 ? v2 : -v2;
    long double P = 2 * L;
    const long double INF = 1e100;
    long double ans = INF;

    auto relax = [&](long double A, long double B) {
        if (fabsl(A) < 1e-18) return;
        long double center = -B / P;
        long long k0 = floor(center);
        for (long long k = k0 - 5; k <= k0 + 5; k++) {
            long double num = B + P * k;
            long double t = num / A;
            if (t >= -1e-12) ans = min(ans, max((long double)0, t));
        }
    };

    relax(u1 - u2, x2 - x1);
    relax(u1 + u2, -x1 - x2);

    if (ans == INF) cout << "Impossible!\n";
    else cout << fixed << setprecision(5) << (double)ans << '\n';
    return 0;
}
```
