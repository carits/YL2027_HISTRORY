# P0060 Remove Prefix 题解

## 一、题意简化

给定数组 a。可以删除一个前缀，使剩下的后缀中所有元素互不相同。

## 二、算法设计

从右向左扫描，维护当前后缀中出现过的元素。

一旦遇到已经出现过的元素，说明从这个位置开始的后缀已经有重复，必须删掉当前位置及其左侧所有元素。

## 三、复杂度分析

每组 O(n)。

## 四、易错点

- 没有严格按照多组数据格式读取。
- 下标从 0 开始和从 1 开始混用。
- 只过了样例，没有检查边界情况。
- 输出格式多空格或少换行。

## 五、参考代码

见 [AC代码.cpp](AC代码.cpp)。
