# P0088 Divisive Battle 题解

## 一、题意简化

本题可以归入 **博弈论**。核心任务是：根据输入限制，选择合适的数据结构或算法，避免直接写出超出复杂度的暴力做法。

## 二、思路分析

无法继续拆的数是 $1$ 或质数。把每个数压缩成“唯一质因子底数”：若含多个不同质因子则记为 $-1$，若为 $p^k$ 则记为 $p$，$1$ 记为 $1$。若原数组已有序，Bob 赢；否则若存在 $-1$ 或压缩数组无序，Alice 能在第一步制造必胜局面，否则 Bob 赢。

## 三、算法步骤

1. 读入题目数据。
2. 根据题意维护必要的状态或预处理信息。
3. 按照上述思路完成计算。
4. 输出题目要求的答案，注意空格、换行和特殊输出格式。

## 四、正确性说明

算法直接围绕题目中的不变量或等价转化进行：每一步维护的状态都与原问题中的真实状态一一对应；当状态更新结束时，得到的就是题目要求的最终结果。对于排序、枚举、模拟、动态规划或数学转化类题目，代码中的比较规则、转移规则或公式均来自题意本身，因此不会改变答案含义。

## 五、复杂度分析

$O(n\log V)$ 或预处理后近似 $O(n\log V)$。

## 六、参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int V = 1000000;
int spf[V + 1];

int prime_base(int x) {
    if (x == 1) return 1;
    int first = 0;
    while (x > 1) {
        int p = spf[x];
        if (!first) first = p;
        else if (first != p) return -1;
        while (x % p == 0) x /= p;
    }
    return first;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    for (int i = 2; i <= V; i++) if (!spf[i]) {
        for (long long j = i; j <= V; j += i) if (!spf[j]) spf[j] = i;
    }

    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        vector<int> a(n), b(n);
        for (int &x : a) cin >> x;
        for (int i = 0; i < n; i++) b[i] = prime_base(a[i]);
        if (is_sorted(a.begin(), a.end())) cout << "Bob\n";
        else if (find(b.begin(), b.end(), -1) != b.end()) cout << "Alice\n";
        else if (is_sorted(b.begin(), b.end())) cout << "Bob\n";
        else cout << "Alice\n";
    }
    return 0;
}
```
