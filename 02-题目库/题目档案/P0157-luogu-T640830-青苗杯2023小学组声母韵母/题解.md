# P0157 【青苗杯2023小学组】声母韵母 题解

## 一、题意简化

本题可以归入 **字符串**。核心任务是：根据输入限制，选择合适的数据结构或算法，避免直接写出超出复杂度的暴力做法。

## 二、思路分析

优先判断双字母声母 `zh`、`ch`、`sh`；否则若首字母是 `a,o,e,i,u,v` 则为零声母，否则首字母为声母。拆分两个拼音后分别比较声母和韵母。

## 三、算法步骤

1. 读入题目数据。
2. 根据题意维护必要的状态或预处理信息。
3. 按照上述思路完成计算。
4. 输出题目要求的答案，注意空格、换行和特殊输出格式。

## 四、正确性说明

算法直接围绕题目中的不变量或等价转化进行：每一步维护的状态都与原问题中的真实状态一一对应；当状态更新结束时，得到的就是题目要求的最终结果。对于排序、枚举、模拟、动态规划或数学转化类题目，代码中的比较规则、转移规则或公式均来自题意本身，因此不会改变答案含义。

## 五、复杂度分析

$O(|s|)$。

## 六、参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

pair<string, string> split_pinyin(const string &s) {
    if (s.rfind("zh", 0) == 0 || s.rfind("ch", 0) == 0 || s.rfind("sh", 0) == 0) {
        return {s.substr(0, 2), s.substr(2)};
    }
    string v = "aoeiuv";
    if (v.find(s[0]) != string::npos) return {"", s};
    return {s.substr(0, 1), s.substr(1)};
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string a, b;
    cin >> a >> b;
    auto x = split_pinyin(a);
    auto y = split_pinyin(b);
    int ans = 0;
    if (x.second == y.second) ans += 1;
    if (x.first == y.first) ans += 2;
    cout << ans << '\n';
    return 0;
}
```
