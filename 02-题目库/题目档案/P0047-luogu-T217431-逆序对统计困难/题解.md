# P0047 逆序对统计（困难） 题解

## 一、题意简化

本题可以归入 **分治**。核心任务是：根据输入限制，选择合适的数据结构或算法，避免直接写出超出复杂度的暴力做法。

## 二、思路分析

归并排序时，若右半部分元素先于左半部分剩余元素放入临时数组，则它与左半部分剩余所有元素构成逆序对。

## 三、算法步骤

1. 读入题目数据。
2. 根据题意维护必要的状态或预处理信息。
3. 按照上述思路完成计算。
4. 输出题目要求的答案，注意空格、换行和特殊输出格式。

## 四、正确性说明

算法直接围绕题目中的不变量或等价转化进行：每一步维护的状态都与原问题中的真实状态一一对应；当状态更新结束时，得到的就是题目要求的最终结果。对于排序、枚举、模拟、动态规划或数学转化类题目，代码中的比较规则、转移规则或公式均来自题意本身，因此不会改变答案含义。

## 五、复杂度分析

$O(n\log n)$。

## 六、参考代码

```cpp
#include <bits/stdc++.h>
using namespace std;

long long merge_count(vector<long long> &a, vector<long long> &tmp, int l, int r) {
    if (r - l <= 1) return 0;
    int m = (l + r) >> 1;
    long long ans = merge_count(a, tmp, l, m) + merge_count(a, tmp, m, r);
    int i = l, j = m, k = l;
    while (i < m || j < r) {
        if (j == r || (i < m && a[i] <= a[j])) {
            tmp[k++] = a[i++];
        } else {
            ans += m - i;
            tmp[k++] = a[j++];
        }
    }
    for (int p = l; p < r; p++) a[p] = tmp[p];
    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    vector<long long> a(n), tmp(n);
    for (auto &x : a) cin >> x;
    cout << merge_count(a, tmp, 0, n) << '\n';
    return 0;
}
```
